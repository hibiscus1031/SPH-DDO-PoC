# CA-03 change record

## Prospective repair

CA-03 was created after the permanent
`DDO01C_H2_UNRESOLVED_CONTRACT_GAP` decision and before any DDO-01C-R registry,
target, slope, figure, or classification. It does not relabel the stopped
DDO-01C stage.

The amendment freezes only H2 executability: formal families, response and
uncertainty bounds, local slopes, tracks, replicates, exact numeric design
slices, deterministic registry mappings, monotonicity and dispersion gates,
verdict mappings, and descriptive support-ratio/relative-effect diagnostics.

## Unchanged contracts

- DDO-00 qualitative H2 requirements and thresholds are unchanged.
- CA-01 `U_num` and `U_round` are unchanged.
- CA-02 scalar/vector case RMS is unchanged.
- All prior terminal states remain permanent.
- No H3-H6 or model work is authorized.

## Evidence separation

No historical or real SPH target magnitude is used in the implementation tests
or registry selection. DDO-01B-R serves only as historical evidence that the
five components are H1-eligible. DDO-01C-R must create fresh cases and rerun all
CA-01 audits.
