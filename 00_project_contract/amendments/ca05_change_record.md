# CA-05 change record

## Trigger

The DDO-01E semantic precheck found frozen H3/H4 scientific gates but incomplete
operational semantics. Missing choices included lineage identity, feature
scalarization, zero-IQR handling, NN baseline/weights, coverage radius,
conditional-variance estimator, fold/case aggregation, bootstrap protocol,
oracle hyperparameters, exact locality construction, H4 paired equivalence, and
component-to-project mapping.

## Prospective repair

CA-05 freezes those choices without changing DDO-00 numeric H3/H4 thresholds.
It also resolves the pair-only versus particle-response rung interface and maps
the authorized DDO-01E L0-L3 ladder to deterministic observable-only graph
summaries.

## Outcome blindness

Before CA-05 freeze, no DDO-01E target archive was opened and no target metric,
regression, predictive score, locality result, or target subspace was computed.
Only target-free registries, observable/reference schemas, manifests, contracts,
and prior audit reports were inspected.

## Authorization boundary

After synthetic qualification and manifest freeze, CA-05 authorizes only the
DDO-01E non-neural development diagnosis described in its contract. Neural
training, H5/H6, sealed evidence, architecture selection, time integration,
rollout, and solver claims remain prohibited.

