# Manuscript v0.3 figure captions

No figure is generated in P2. These captions define the publication role of each planned figure and must be paired only with frozen source artifacts.

## Figure 1. Pre-learning qualification hierarchy and stopping logic

The fixed-time spatial defect proceeds through numerical/reference qualification, signal resolvability (H1), controlled scaling (H2), and observable identifiability (H3). Locality (H4) is conditional on H3, while representation learning (H5) and generalization (H6) require the upstream stages. The tested route stopped after all three primary dynamic mappings failed fresh H3. H4 was not qualified, H5 and H6 were not authorized, and no neural training occurred. The figure must not depict H4–H6 as failed experiments.

## Figure 2. Fixed-time defect definition and observable-reference firewall

The defect is \(d_h^*=\mathcal{R}_h\mathcal{L}(q^*)-\mathcal{L}_h(\mathcal{R}_h q^*)\), with a positive-additive sign convention. Closed-form continuum derivatives construct the reference target, while the low-cost SPH state supplies deployment-compatible observables. Reference and observable archives remain physically separate, and no reference-minus-low-cost or target-derived quantity enters the input path. High-resolution SPH must not be shown as truth, and no arrow may carry reference information into feature construction.

## Figure 3. Signal resolvability relative to qualified numerical uncertainty

Componentwise signal-to-uncertainty ratios \(R_c=T_c/U_c\) and stratified-bootstrap lower bounds \(L_{95,c}\) are shown against the frozen limits \(R_c\geq10\) and \(L_{95,c}>5\). All five components pass over their qualified excited-case scopes. Analytically unexcited pairs are excluded rather than represented as zeros. The panel establishes resolvability only and must not imply scaling, identifiability, or learnability.

## Figure 4. Componentwise scaling and disorder sensitivity

Refinement and spectral qualification are shown at the canonical support ratio \(h/\Delta x=4\) for regular and tested jittered configurations. Density rate passes both tracks in both scopes. Pressure-gradient acceleration, viscosity-Laplacian acceleration, and derived total acceleration pass only the regular scope because jittered refinement does not satisfy the frozen gate. Interpolation density does not satisfy regular or jittered scaling. Any plotted local slopes must be labelled descriptive, not convergence orders.

## Figure 5. Mechanism-stratified development atlas

The 512-case analytical atlas is balanced across single-mode, multimode, directional/mechanism, and controlled-disorder families, with 128 cases per family. Matched disorder blocks retain variation in support ratio and perturbation. All cases are labelled development evidence and later consumed observable-design evidence. The figure must not present this atlas as fresh requalification evidence or imply identifiability from construction alone.

## Figure 6. Initial identifiability assessment with qualified coverage

Nearest-neighbour disagreement, conditional target variance, fixed non-neural oracle error, family robustness, and coverage are shown for the initial C3/L3 representation on 65,536 lineage-separated samples. All three primary dynamic mappings fail at least one frozen H3 criterion, although formal coverage is 0.9526 and exceeds the 0.90 minimum. Favourable individual metrics must remain visible and cannot replace the all-gates decision. The target singular-value diagnostic is excluded from formal H3 and must not be presented as a manifold estimate.

## Figure 7. Failure attribution and prospectively frozen observable redesign

Consumed development evidence is separated from the prospective redesign. Consistency-descriptor ablations show no uniform pressure or viscosity rescue across disorder strata. Directional augmentation and component-specific combinations are supported only for a fresh test, whereas higher moments and derivative proxies remain inconclusive. The redesign freezes 30 reference-free descriptors and excludes four design-only fields before fresh target access. No panel may label attribution as a renewed H3 pass.

## Figure 8. Fresh observable-identifiability requalification and route closure

The expanded representation is evaluated on 384 new cases, balanced as 96 per family, with zero development-lineage overlap and 49,152 formal samples. Fresh metrics are shown against all frozen H3 limits for density rate, pressure-gradient acceleration, and viscosity-Laplacian acceleration. The observable-frame fallback count is 515,904 of 627,264 particle environments (82.246710%). All three mappings fail fresh H3, so locality is not interpreted and no neural stage is initiated. The figure must not imply universal failure of temporal, equivariant, graph-based, or alternative-observable routes.

